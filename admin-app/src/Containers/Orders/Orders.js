import React from 'react'
import Layout from '../../Components/Layout/Layout'

const Orders = (props) => {
    return (
        <Layout sidebar>
            ordres
        </Layout>
    )
}

export default Orders
